import styles from '../css-components/AboutMe.module.css';

export default function AboutMe(){
    return(
        <div className={`global-AboutMe" ${styles.AboutMe}`}>
            <h1>About Me</h1>
        </div>
    )
}